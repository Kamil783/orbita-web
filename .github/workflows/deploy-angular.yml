name: Deploy Angular to VPS

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install deps
        run: npm ci

      - name: Build
        run: npm run build -- --configuration production

      - name: Install rsync
        run: sudo apt-get update && sudo apt-get install -y rsync

      - name: Start ssh-agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.VPS_SSH_KEY }}

      - name: Add host to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to /var/www/app
        run: |
          echo "Dist contents:"
          ls -la dist
          ls -la dist/orbita-web

          # Деплой именно orbita-web (твой build output)
          rsync -az --delete dist/orbita-web/ ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:/var/www/app/
